# Setting listen-address causes dnsmasq to ignore the local-service restriction,
# allowing queries from non-directly-attached networks (e.g. router at 192.168.0.1
# querying the Pi-hole container via Docker port mapping).
listen-address=0.0.0.0
